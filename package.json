{
  "name": "cron-parser",
  "version": "1.0.0",
  "description": "Node cron parser",
  "main": "./lib/index.js",
  "bin": {
    "cron-cli": "./lib/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "local": "sudo npm uninstall -g --force && sudo npm install -g --force",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Sorry didn't have time to write tests in 3 hours\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.10.3",
    "@types/yargs": "^17.0.3",
    "ts-node": "^10.3.0",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "yargs": "^17.2.1"
  }
}
